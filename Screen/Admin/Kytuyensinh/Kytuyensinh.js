import React from "react";
import { View, Text } from "react-native";

const Kytuyensinh = ({ route, navigation }) => {
  const data = route.params;
  // console.log(data);
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Kytuyensinh;
